# Copy this file to env.hostinger on the VPS and fill real values there.
# Do not commit env.hostinger.

# Public DNS name that points at the VPS, without scheme.
# Example: api.example.com
DECLUTTER_PUBLIC_HOST=api.your-domain.example

# Production default. Use scaffold only behind a VPN/private access gate.
DECLUTTER_AUTH_MODE=strict
FIREBASE_PROJECT_ID=your-firebase-project-id
DECLUTTER_CORS_ALLOW_ORIGINS=https://your-launch-frontend.example

# Production readiness expects S3 storage and a bucket.
DECLUTTER_STORAGE_BACKEND=s3
DECLUTTER_S3_BUCKET=your-private-upload-bucket
DECLUTTER_S3_PREFIX=intake
AWS_REGION=us-east-1

# Current backend scaffold still returns deterministic starter output unless
# a real provider adapter is added/configured.
DECLUTTER_MODEL_PROVIDER=launch-model-provider

# eBay credentials stay only on the VPS/secret manager.
EBAY_CLIENT_ID=your-ebay-client-id
EBAY_CLIENT_SECRET=your-ebay-client-secret

# Persistent container paths. The compose file mounts /data as a named volume.
DECLUTTER_SESSION_DB_PATH=/data/declutter_ai_sessions.sqlite3
DECLUTTER_UPLOAD_DIR=/data/uploads

# Demo-only scaffold auth. Uncomment only for a private demo behind an access gate.
# DECLUTTER_AUTH_MODE=scaffold
# DECLUTTER_TEST_ID_TOKEN=replace-with-a-long-random-demo-token
# DECLUTTER_TEST_APP_CHECK_TOKEN=replace-with-a-long-random-demo-app-check-token
# DECLUTTER_STORAGE_BACKEND=local
# DECLUTTER_CORS_ALLOW_ORIGINS=https://private-demo-frontend.example
